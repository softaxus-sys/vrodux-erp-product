Save the Vrodux logo here as:  vrodux-logo.png

The <BrandLogo> component (src/components/brand/brand-logo.tsx) loads /vrodux-logo.png.
Until the PNG is present, the app shows a graceful icon + "Vrodux" wordmark fallback.

Recommended: a transparent-background PNG, ~600x160px (the full "V rodux — ERP Solution" lockup).
